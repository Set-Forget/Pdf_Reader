export class ChatFile {
    constructor(
        name,
        id,
        url,
        type,
        chatId) {
        this.name = name,
        this.driveId = id,
        this.url = url,
        this.type = type,
        this.chatId = chatId
    }
}